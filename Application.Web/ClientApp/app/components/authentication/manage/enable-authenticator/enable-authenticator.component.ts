import { Component } from '@angular/core';

@Component({
    selector: 'enable-authenticator',
    templateUrl: './enable-authenticator.component.html',
    styleUrls: ['./enable-authenticator.component.css']
})
export class EnableAuthenticatorComponent {
}
